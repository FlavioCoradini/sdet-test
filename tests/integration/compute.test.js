const request = require("supertest");
const app = require("../../src/app");

describe("compute", () => {
  test("compute should return 200", () => {
    expect(true).toBe(true);
  });

  test("compute should return 400", () => {
    expect(true).toBe(true);
  });
});
